<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserConnection.admin_table"></activity>
        <activity android:name=".UserConnection.stu_table" />
        <activity android:name=".UserConnection.tea_table" />
        <activity android:name=".UserConnection.description" />
        <activity android:name=".UserConnection.admin_input" />
        <activity android:name=".UserConnection.tea_input" />
        <activity android:name=".UserConnection.stu_input" />
        <activity android:name=".UserConnection.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>